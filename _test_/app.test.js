import {performAction} from '../src/client/js/app'

describe("Test the submit function", () => {
    test("Test the performAction() function", () => {
        expect(performAction).toBeDefined();
    })
})